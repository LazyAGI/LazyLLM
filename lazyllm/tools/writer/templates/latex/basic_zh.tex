$document-metadata.latex()$
$passoptions.latex()$
\documentclass[UTF8,zihao=-4$for(classoption)$,$classoption$$endfor$]{ctexart}

\usepackage{xcolor}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
$if(cancel)$
\usepackage{cancel}
$endif$
\setcounter{secnumdepth}{5}
$fonts.latex()$
$font-settings.latex()$
$common.latex()$

\usepackage{setspace}
\usepackage{enumitem}
\usepackage{fancyhdr}
\setstretch{1.35}
\setlist{nosep}
\graphicspath{{assets/}}
\pagestyle{plain}
\newcommand{\taskunchecked}{\ensuremath{\square}}
\newcommand{\taskchecked}{\ensuremath{\boxtimes}}
\newcounter{codeblock}
\renewcommand{\thecodeblock}{\arabic{codeblock}}
\providecommand*{\codeblockname}{代码}
\providecommand*{\codeblockcaptionseparator}{：}
\newcommand{\writerinternalref}[1]{（\autoref{#1}）}

$for(header-includes)$
$header-includes$
$endfor$
$after-header-includes.latex()$
$hypersetup.latex()$
\renewcommand*{\sectionautorefname}{章节}
\renewcommand*{\subsectionautorefname}{小节}
\renewcommand*{\subsubsectionautorefname}{小节}
\renewcommand*{\figureautorefname}{图}
\renewcommand*{\tableautorefname}{表}
\renewcommand*{\equationautorefname}{公式}
\providecommand*{\codeblockautorefname}{代码块}

$if(title)$
\title{$title$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$else$
\author{}
$endif$
$if(date)$
\date{$date$}
$else$
\date{}
$endif$

\begin{document}
$if(title)$
\maketitle
$endif$

$for(include-before)$
$include-before$

$endfor$
$if(toc)$
$if(toc-title)$
\renewcommand*\contentsname{$toc-title$}
$endif$
{
\setcounter{tocdepth}{$toc-depth$}
\tableofcontents
}
$endif$

$body$

$for(include-after)$
$include-after$

$endfor$
\end{document}
